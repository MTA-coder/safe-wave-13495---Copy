<?php

namespace App\Enums;

use BenSampo\Enum\Enum;

final class OrderHallEnum extends Enum
{
    const watches_count = "watches_count";
    const rates_avg_rate = "rates_avg_rate";
    const resereves_count = "resereves_count";
}